
<% if user_signed_in? %>
  Logged in as <strong><h1>Hello, <%= @current_user %></h1></strong>.
  <%= link_to 'Edit profile', edit_user_registration_path, :class => 'navbar-link' %> |
  <%= link_to "Logout", destroy_user_session_path, data: { turbo_method: :delete, confirm: "Are you sure?", turbo_confirm: "Are you sure?" }, :class => 'navbar-link'  %>
<% else %>
<h1>Welcome, Guest</h1>

<% end %>
